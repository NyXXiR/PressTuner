import { PRESS_AI_SAMPLE_SCENARIOS } from "@/domain/press-ai-debugger/sampleManifest";
export function PressAiSampleScenarios(props: { selectedId: string | null; onSelect: (scenario: (typeof PRESS_AI_SAMPLE_SCENARIOS)[number]) => void; onAdd: (scenario: (typeof PRESS_AI_SAMPLE_SCENARIOS)[number]) => void; adding: boolean }) { return <fieldset><legend className="text-sm font-black">샘플 시나리오</legend><div className="mt-2 grid gap-3 lg:grid-cols-2">{PRESS_AI_SAMPLE_SCENARIOS.map((scenario) => <article key={scenario.id} className={`rounded-xl border p-4 ${props.selectedId === scenario.id ? "border-primary bg-primary/5" : "border-border"}`}><h4 className="font-black">{scenario.title}</h4><p className="mt-2 text-sm"><strong>목적:</strong> {scenario.purpose}</p><p className="mt-2 text-sm text-muted-foreground"><strong>권장 질문:</strong> {scenario.recommendedPrompt}</p><p className="mt-2 text-sm text-muted-foreground"><strong>예상 결과:</strong> {scenario.expectedBehavior}</p><div className="mt-3 flex flex-wrap gap-2"><button type="button" aria-pressed={props.selectedId === scenario.id} onClick={() => props.onSelect(scenario)} className="min-h-11 rounded-lg border border-border px-3 text-sm font-bold">이 시나리오로 설정</button><button type="button" onClick={() => props.onAdd(scenario)} disabled={props.adding} className="min-h-11 rounded-lg bg-primary px-3 text-sm font-bold text-primary-foreground disabled:opacity-50">팀에 샘플 추가</button>{scenario.assets.map((asset) => <a key={asset.id} href={asset.path} download={asset.uploadFilename} className="inline-flex min-h-11 items-center rounded-lg px-3 text-sm font-bold underline">PDF 받기</a>)}</div></article>)}</div></fieldset>; }

