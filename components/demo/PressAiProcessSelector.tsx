import { PRESS_AI_PROCESS_IDS, getPressAiProcessDefinition, type PressAiProcessId } from "@/domain/press-ai-debugger/processRegistry";
export function PressAiProcessSelector(props: { value: PressAiProcessId; onChange: (value: PressAiProcessId) => void; disabled?: boolean }) { return <fieldset className="mt-5" disabled={props.disabled}><legend className="text-sm font-black">실행할 Press AI 프로세스</legend><div className="mt-2 grid gap-3 sm:grid-cols-2">{PRESS_AI_PROCESS_IDS.map((id) => { const process = getPressAiProcessDefinition(id); return <label key={id} className={`min-h-11 cursor-pointer rounded-xl border p-4 ${props.value === id ? "border-primary bg-primary/10" : "border-border"}`}><input type="radio" name="press-ai-process" value={id} checked={props.value === id} onChange={() => props.onChange(id)} className="mr-3"/><strong>{process.label}</strong><span className="mt-1 block text-sm text-muted-foreground">{process.description}</span></label>; })}</div></fieldset>; }

