export function PressAiProcessRunHistory(props: { runs: Array<{ id: string; processId: string; status: string; articleId: string | null; createdAt: string }>; onOpen: (id: string) => void }) { return <section className="mt-6"><h3 className="font-black">내 저장 실행 기록</h3>{props.runs.length === 0 ? <p className="mt-2 text-sm text-muted-foreground">저장된 실행이 없습니다.</p> : <ul className="mt-2 grid gap-2">{props.runs.map((run) => <li key={run.id} className="flex flex-wrap items-center justify-between gap-2 rounded-lg border border-border p-3 text-sm"><span><strong>{run.processId}</strong> · {run.status} · {new Date(run.createdAt).toLocaleString("ko-KR")}{run.articleId ? ` · 문서 ${run.articleId}` : ""}</span><button type="button" onClick={() => props.onOpen(run.id)} className="min-h-11 rounded-lg border border-border px-3 font-bold">저장 기록 열기</button></li>)}</ul>}</section>; }

