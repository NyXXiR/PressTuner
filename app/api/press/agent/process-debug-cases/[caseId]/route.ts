import { NextResponse } from "next/server"; import { requireTeamContext } from "@/lib/auth"; import { getDebugCase } from "@/lib/services/press-ai-debugger/caseService"; import { apiError } from "@/lib/utils/api";
export const runtime = "nodejs"; export const dynamic = "force-dynamic"; const headers = { "Cache-Control": "no-store" };
export async function GET(_: Request, context: { params: Promise<{ caseId: string }> }) { try { const { team } = await requireTeamContext(); const { caseId } = await context.params; return NextResponse.json({ case: await getDebugCase({ teamId: team.id, caseId }) }, { headers }); } catch (error: any) { const status = error?.status ?? 500; return NextResponse.json(apiError(error?.code ?? error?.message ?? "PRESS_AI_DEBUG_CASE_DETAIL_FAILED", error?.message ?? "Failed", status).body, { status, headers }); } }
